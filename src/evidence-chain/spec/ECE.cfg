\* TLC Model Configuration for ECE.tla
\* Targets: 18,432 states / 9,216 distinct states
\* 3 claims × 3 operators × 8 truth-states × 2 lock states × 2 author options

SPECIFICATION Spec

CONSTANTS
  Claims    = {c1, c2, c3}
  Operators = {op1, op2, op3}

INVARIANTS
  TypeOK
  StateIsReachable
  ChainMonotone
  TerminalIsAbsorbing
  NoSelfVerify
  ChallengeBlocks
