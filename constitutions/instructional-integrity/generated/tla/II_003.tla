---------------------------- MODULE II_003 ----------------------------
\* GENERATED by the TLC-SL compiler from II-003 — Cognitive load ceiling requires segmentation.
\* Article Instructional Integrity / Cognitive Load Theory. Enforcement BLOCK.
\* STATUS: emitted, NOT yet checked by TLC in this build. See tla.mjs header.
EXTENDS TLC

VARIABLES breach
vars == << breach >>

TypeOK ==
  /\ breach \in { "ok", "violated" }

Init ==
  /\ breach = "ok"

Op_PRESENT_STEP ==
  \E load \in { "within", "exceeds" }, segmented \in { "yes", "no" } :
    /\ (~((load = "exceeds") /\ (segmented = "no")))
    /\ breach' = (IF ((load = "exceeds") /\ (segmented = "no")) THEN "violated" ELSE breach)

Next ==
  \/ Op_PRESENT_STEP

Spec == Init /\ [][Next]_vars

\* Safety property (Constitution Instructional Integrity / Cognitive Load Theory):
Safety == (breach = "ok")
=============================================================================
