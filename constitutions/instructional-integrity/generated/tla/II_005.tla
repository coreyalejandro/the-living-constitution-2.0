---------------------------- MODULE II_005 ----------------------------
\* GENERATED by the TLC-SL compiler from II-005 — Accessible rendering required before delivery.
\* Article Instructional Integrity / UDL. Enforcement BLOCK.
\* STATUS: emitted, NOT yet checked by TLC in this build. See tla.mjs header.
EXTENDS TLC

VARIABLES a11y, delivery
vars == << a11y, delivery >>

TypeOK ==
  /\ a11y \in { "absent", "present" }
  /\ delivery \in { "blocked", "delivered" }

Init ==
  /\ a11y = "absent"
  /\ delivery = "blocked"

Op_ADD_ACCESSIBLE_RENDERING ==
  /\ TRUE
  /\ a11y' = "present"
  /\ UNCHANGED << delivery >>

Op_DELIVER ==
  /\ (a11y = "present")
  /\ delivery' = "delivered"
  /\ UNCHANGED << a11y >>

Next ==
  \/ Op_ADD_ACCESSIBLE_RENDERING
  \/ Op_DELIVER

Spec == Init /\ [][Next]_vars

\* Safety property (Constitution Instructional Integrity / UDL):
Safety == ((delivery = "delivered") => (a11y = "present"))
=============================================================================
