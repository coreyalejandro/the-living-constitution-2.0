---------------------------- MODULE INV_022 ----------------------------
\* GENERATED by the TLC-SL compiler from INV-022 — Every break-glass override is logged.
\* Article VIII.3. Enforcement BLOCK.
\* STATUS: emitted, NOT yet checked by TLC in this build. See tla.mjs header.
EXTENDS TLC

VARIABLES logged, overridden
vars == << logged, overridden >>

TypeOK ==
  /\ logged \in { "no", "yes" }
  /\ overridden \in { "no", "yes" }

Init ==
  /\ logged = "no"
  /\ overridden = "no"

Op_LOG_OVERRIDE ==
  /\ TRUE
  /\ logged' = "yes"
  /\ UNCHANGED << overridden >>

Op_OVERRIDE ==
  /\ (logged = "yes")
  /\ overridden' = "yes"
  /\ UNCHANGED << logged >>

Next ==
  \/ Op_LOG_OVERRIDE
  \/ Op_OVERRIDE

Spec == Init /\ [][Next]_vars

\* Safety property (Constitution VIII.3):
Safety == ((overridden = "yes") => (logged = "yes"))
=============================================================================
