---------------------------- MODULE INV_020 ----------------------------
\* GENERATED by the TLC-SL compiler from INV-020 — Evidence is append-only.
\* Article VIII.3 / V.4. Enforcement BLOCK.
\* STATUS: emitted, NOT yet checked by TLC in this build. See tla.mjs header.
EXTENDS TLC

VARIABLES log
vars == << log >>

TypeOK ==
  /\ log \in { "intact", "mutated" }

Init ==
  /\ log = "intact"

Op_APPEND ==
  /\ TRUE
  /\ UNCHANGED << log >>

Op_MODIFY ==
  /\ FALSE
  /\ log' = "mutated"

Op_DELETE ==
  /\ FALSE
  /\ log' = "mutated"

Next ==
  \/ Op_APPEND
  \/ Op_MODIFY
  \/ Op_DELETE

Spec == Init /\ [][Next]_vars

\* Safety property (Constitution VIII.3 / V.4):
Safety == (log = "intact")
=============================================================================
