---------------------------- MODULE INV_023 ----------------------------
\* GENERATED by the TLC-SL compiler from INV-023 — PII requires authorization.
\* Article VIII.3 / I. Enforcement BLOCK.
\* STATUS: emitted, NOT yet checked by TLC in this build. See tla.mjs header.
EXTENDS TLC

VARIABLES leak
vars == << leak >>

TypeOK ==
  /\ leak \in { "none", "leaked" }

Init ==
  /\ leak = "none"

Op_EMIT ==
  \E contains_pii \in { "yes", "no" }, authorized \in { "yes", "no" } :
    /\ (~((contains_pii = "yes") /\ (authorized = "no")))
    /\ leak' = (IF ((contains_pii = "yes") /\ (authorized = "no")) THEN "leaked" ELSE leak)

Next ==
  \/ Op_EMIT

Spec == Init /\ [][Next]_vars

\* Safety property (Constitution VIII.3 / I):
Safety == (leak = "none")
=============================================================================
