---------------------------- MODULE INV_002 ----------------------------
\* GENERATED by the TLC-SL compiler from INV-002 — Contract activation requires schema validation.
\* Article VIII.1. Enforcement BLOCK.
\* STATUS: emitted, NOT yet checked by TLC in this build. See tla.mjs header.
EXTENDS TLC

VARIABLES schema, contract
vars == << schema, contract >>

TypeOK ==
  /\ schema \in { "invalid", "valid" }
  /\ contract \in { "draft", "active" }

Init ==
  /\ schema = "invalid"
  /\ contract = "draft"

Op_VALIDATE ==
  /\ TRUE
  /\ schema' = "valid"
  /\ UNCHANGED << contract >>

Op_ACTIVATE ==
  /\ (schema = "valid")
  /\ contract' = "active"
  /\ UNCHANGED << schema >>

Next ==
  \/ Op_VALIDATE
  \/ Op_ACTIVATE

Spec == Init /\ [][Next]_vars

\* Safety property (Constitution VIII.1):
Safety == ((contract = "active") => (schema = "valid"))
=============================================================================
