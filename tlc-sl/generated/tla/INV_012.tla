---------------------------- MODULE INV_012 ----------------------------
\* GENERATED by the TLC-SL compiler from INV-012 — Public surface requires a public_display_status.
\* Article VIII.2. Enforcement BLOCK.
\* STATUS: emitted, NOT yet checked by TLC in this build. See tla.mjs header.
EXTENDS TLC

VARIABLES pds, surface
vars == << pds, surface >>

TypeOK ==
  /\ pds \in { "unset", "set" }
  /\ surface \in { "private", "public" }

Init ==
  /\ pds = "unset"
  /\ surface = "private"

Op_SET_PDS ==
  /\ TRUE
  /\ pds' = "set"
  /\ UNCHANGED << surface >>

Op_PUBLISH ==
  /\ (pds = "set")
  /\ surface' = "public"
  /\ UNCHANGED << pds >>

Next ==
  \/ Op_SET_PDS
  \/ Op_PUBLISH

Spec == Init /\ [][Next]_vars

\* Safety property (Constitution VIII.2):
Safety == ((surface = "public") => (pds = "set"))
=============================================================================
