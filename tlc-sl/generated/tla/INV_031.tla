---------------------------- MODULE INV_031 ----------------------------
\* GENERATED by the TLC-SL compiler from INV-031 — Empty unverified on an unrun suite is a violation.
\* Article VIII.4 / V.2. Enforcement BLOCK.
\* STATUS: emitted, NOT yet checked by TLC in this build. See tla.mjs header.
EXTENDS TLC

VARIABLES breach
vars == << breach >>

TypeOK ==
  /\ breach \in { "ok", "violated" }

Init ==
  /\ breach = "ok"

Op_PUBLISH_VNT ==
  \E suite_run \in { "yes", "no" }, unverified \in { "empty", "filled" } :
    /\ (~((suite_run = "no") /\ (unverified = "empty")))
    /\ breach' = (IF ((suite_run = "no") /\ (unverified = "empty")) THEN "violated" ELSE breach)

Next ==
  \/ Op_PUBLISH_VNT

Spec == Init /\ [][Next]_vars

\* Safety property (Constitution VIII.4 / V.2):
Safety == (breach = "ok")
=============================================================================
