---------------------------- MODULE INV_010 ----------------------------
\* GENERATED by the TLC-SL compiler from INV-010 — Working requires verified scope.
\* Article VIII.2 / II.4. Enforcement BLOCK.
\* STATUS: emitted, NOT yet checked by TLC in this build. See tla.mjs header.
EXTENDS TLC

VARIABLES status, verified_scope
vars == << status, verified_scope >>

TypeOK ==
  /\ status \in { "unverified", "partial", "working" }
  /\ verified_scope \in { "yes", "no" }

Init ==
  /\ status = "unverified"
  /\ verified_scope = "no"

Op_SET_VERIFIED_SCOPE ==
  /\ TRUE
  /\ verified_scope' = "yes"
  /\ UNCHANGED << status >>

Op_PROMOTE_PARTIAL ==
  /\ TRUE
  /\ status' = "partial"
  /\ UNCHANGED << verified_scope >>

Op_PROMOTE_WORKING ==
  /\ (verified_scope = "yes")
  /\ status' = "working"
  /\ UNCHANGED << verified_scope >>

Next ==
  \/ Op_SET_VERIFIED_SCOPE
  \/ Op_PROMOTE_PARTIAL
  \/ Op_PROMOTE_WORKING

Spec == Init /\ [][Next]_vars

\* Safety property (Constitution VIII.2 / II.4):
Safety == ((status = "working") => (verified_scope = "yes"))
=============================================================================
