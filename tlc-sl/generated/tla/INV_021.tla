---------------------------- MODULE INV_021 ----------------------------
\* GENERATED by the TLC-SL compiler from INV-021 — Every halt event produces an evidence entry.
\* Article VIII.3. Enforcement BLOCK.
\* STATUS: emitted, NOT yet checked by TLC in this build. See tla.mjs header.
EXTENDS TLC

VARIABLES logged, halted
vars == << logged, halted >>

TypeOK ==
  /\ logged \in { "no", "yes" }
  /\ halted \in { "no", "yes" }

Init ==
  /\ logged = "no"
  /\ halted = "no"

Op_LOG_HALT_EVIDENCE ==
  /\ TRUE
  /\ logged' = "yes"
  /\ UNCHANGED << halted >>

Op_RAISE_HALT ==
  /\ (logged = "yes")
  /\ halted' = "yes"
  /\ UNCHANGED << logged >>

Next ==
  \/ Op_LOG_HALT_EVIDENCE
  \/ Op_RAISE_HALT

Spec == Init /\ [][Next]_vars

\* Safety property (Constitution VIII.3):
Safety == ((halted = "yes") => (logged = "yes"))
=============================================================================
