---------------------------- MODULE INV_003 ----------------------------
\* GENERATED by the TLC-SL compiler from INV-003 — Step completion requires human approval.
\* Article VIII.1 / IV.4. Enforcement BLOCK.
\* STATUS: emitted, NOT yet checked by TLC in this build. See tla.mjs header.
EXTENDS TLC

VARIABLES approval, step
vars == << approval, step >>

TypeOK ==
  /\ approval \in { "absent", "present" }
  /\ step \in { "open", "complete" }

Init ==
  /\ approval = "absent"
  /\ step = "open"

Op_APPROVE ==
  /\ TRUE
  /\ approval' = "present"
  /\ UNCHANGED << step >>

Op_COMPLETE ==
  /\ (approval = "present")
  /\ step' = "complete"
  /\ UNCHANGED << approval >>

Next ==
  \/ Op_APPROVE
  \/ Op_COMPLETE

Spec == Init /\ [][Next]_vars

\* Safety property (Constitution VIII.1 / IV.4):
Safety == ((step = "complete") => (approval = "present"))
=============================================================================
